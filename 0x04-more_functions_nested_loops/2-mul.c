#include "main.h"
/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: thz second number to multiply with first number
 * Return: The result of multiplication x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}

